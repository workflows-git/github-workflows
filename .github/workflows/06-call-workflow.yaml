name: call another workflow
on: 
  push:
    branches:
        - 'main'
jobs:
  feature-pipeline:
    uses: workflows/.github/workflows/nodejs-eks-feature.yaml@main  # Reference local path if in the same repository
    with:
      project: 'roboshop' 
      component: 'catalogue'